You are working on `ralph` — an FSM-driven autonomous-loop CLI in Go, located at `{{.RepoRoot}}`.

The authoritative spec is at `~/.claude/plans/i-want-to-ralph-sequential-cherny.md`. Read it if you need to align on architecture; the README and `docs/` cover principles, and `bd list --type decision` exposes the byob-go-cli design defaults this project follows.

Current iteration: {{.Iter}}. State: `{{.State}}` (previous: `{{.PrevState}}`). Git HEAD: `{{.GitHead}}` (dirty: {{.GitDirty}}).
